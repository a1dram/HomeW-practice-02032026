#include <iostream>

template<class T>
T* merge(const T* a, size_t sa, const T* b, size_t sb, T* c)
{
  const T* a_it = a;
  const T* a_end = a + sa;
  const T* b_it = b;
  const T* b_end = b + sb;
  T* out = c;

  return out;
}
